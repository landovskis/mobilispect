# Sample GTFS for Transit Analysis

Lightweight GTFS fixture to exercise transit-analysis parsing (routes, trips, stop_times).
Intended for local testing and demos; not production quality.

Contains a single agency, one route with two trips (opposite directions), three stops,
and a weekday calendar for simple transit-analysis smoke tests.
Use this for offline runs or quick parsing demos.
