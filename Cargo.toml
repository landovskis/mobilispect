[workspace]
members = ["crates/core", "crates/server", "crates/worker"]
exclude = ["crates/corridor_builder_web"]
resolver = "2"

# Binary tool requirements (not enforced by Cargo — install manually):
#   cargo install cargo-nextest --locked
[workspace.metadata.tools]
cargo-nextest = "0.9.136"
