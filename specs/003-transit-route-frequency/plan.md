# Implementation Plan: Transit Route Frequency Analysis

**Branch**: `003-transit-route-frequency` | **Date**: 2025-11-27 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/003-transit-route-frequency/spec.md`

**Note**: This document is the technical implementation plan generated by `/speckit.plan`. It translates the feature specification into concrete technical decisions, architecture, and design artifacts.

## Summary

This feature enables transit planners to analyze service frequency patterns across regional transit agencies. The system imports GTFS (General Transit Feed Specification) feeds from Transitland API, identifies route variants by analyzing stop patterns, calculates frequency metrics for different time periods, and detects common sections where routes overlap. Users access the analysis through a responsive web interface that displays frequency data organized by region, agency, and route.

**Technical Approach**: Spring Boot backend module using Spring Modulith for bounded context enforcement, PostgreSQL for persistence, Transitland API for feed discovery, Angular frontend with responsive design, comprehensive observability via Grafana Cloud.

## Technical Context

**Language/Version**:

- Backend: Kotlin 2.0+ with Spring Boot 3.x
- Frontend: TypeScript 5.x with Angular 19 LTS

**Primary Dependencies**:

- Backend: Spring Boot, Spring Modulith, Spring Data JPA, Spring WebFlux (WebClient), OneBusAway GTFS library
- Frontend: Angular 19, RxJS 7+, Angular Material for UI components
- Testing: JUnit 5, MockK, Playwright for E2E

**Storage**:

- PostgreSQL 17 for persistent data (regions, agencies, routes, variants, frequencies, imported feeds)
- Redis 8.2 for caching feed data and frequency calculations

**Testing**:

- Backend: JUnit 5 with `@ModuleTest` for module isolation, Testcontainers for PostgreSQL/Redis
- Frontend: Jasmine/Karma for unit tests, Playwright for cross-browser E2E tests
- Contract tests for REST API endpoints using Spring REST Docs

**Target Platform**:

- Backend: Linux server (containerized deployment)
- Frontend: Modern web browsers (Chrome, Firefox, Safari, Edge) - desktop and mobile viewports

**Project Type**: Web application (backend + frontend)

**Performance Goals**:

- Backend API response time: <200ms p95 (constitutional requirement)
- Feed processing: Complete 20-agency region within 5 minutes (SC-005)
- UI response: Load region/agency/route data within 2 seconds (SC-001)
- Frequency calculation accuracy: Within 1 minute of source data (SC-002)

**Constraints**:

- Backend APIs MUST respond within 200ms p95
- System MUST process up to 20 agencies per region within 5 minutes
- UI MUST display up to 100 routes simultaneously without degradation
- System uptime MUST maintain 99.5% during feed operations
- All schedule times stored in UTC, displayed in regional timezone

**Scale/Scope**:

- Support multiple metropolitan regions (10-50 regions initially)
- Process feeds from 20+ transit agencies per region
- Handle 1000+ routes across all agencies
- Store 2 years of historical frequency data
- Single Spring Modulith module (`transit-analysis`)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### Principle I: Code Quality First ✅

- **DRY/YAGNI/SOLID**: Module design follows Single Responsibility (frequency analysis), value classes for all entity IDs (RegionId, AgencyId, RouteId, VariantId)
- **Linting/Formatting**: ktlint for Kotlin, ESLint/Prettier for TypeScript/Angular
- **Static Analysis**: SonarQube integration in CI/CD
- **Code Reviews**: Required for all changes with platform expert approval

### Principle II: Test-Driven Development ✅

- **TDD Approach**: Tests written before implementation for all business logic
- **Coverage Target**: 80%+ coverage required (constitutional mandate)
- **Test Types**: Unit tests (services, domain logic), integration tests (module boundaries, database), contract tests (REST APIs), E2E tests (Playwright)
- **Module Tests**: `@ModuleTest` for transit-analysis module isolation

### Principle III: Cross-Platform UX Consistency ✅

- **Platform**: Responsive web UI only (desktop + mobile browsers)
- **Design System**: Angular Material components for consistent UI
- **Light/Dark Mode**: Required across all views
- **WCAG 2.1 AA**: Accessibility criteria documented in acceptance tests; automated scans (axe) + manual testing for critical flows

### Principle IV: Performance Standards ✅

- **API Response Time**: <200ms p95 enforced via load testing
- **Database Optimization**: Indexes on foreign keys, composite indexes for query patterns, no N+1 queries
- **Caching Strategy**: Redis for computed frequencies and feed metadata
- **Performance Tests**: JMeter/Gatling tests in CI/CD pipeline

### Principle V: Observability & Monitoring ✅

- **Structured Logs**: All feed import/processing operations (FR-023)
- **Metrics**: Processing duration, feed size, variant count, error rates (FR-024)
- **Distributed Traces**: Feed import workflow spans (FR-025)
- **Grafana Cloud**: Centralized observability platform (constitutional requirement)
- **Dashboards**: Feed processing health, frequency calculation performance, API latency

### Principle VI: Architecture Decision Records ✅

- **Required ADRs**:
  - ADR-001: Use Transitland API for feed discovery (vs manual configuration)
  - ADR-002: Hash-based route variant identification (vs agency-provided IDs)
  - ADR-003: OneBusAway GTFS library selection (vs custom parser)
  - ADR-004: Common section detection algorithm design
  - ADR-005: Frequency calculation methodology (average headway by time period)

### Principle VII: Modular Monolith Architecture ✅

- **Module Name**: `transit-analysis`
- **Bounded Context**: Transit feed import, route variant analysis, frequency calculation
- **Public API**: REST endpoints for region/agency/route/frequency queries, feed import operations
- **Events Published**: `FeedImportCompleted`, `FrequencyCalculationCompleted`, `RouteVariantIdentified`
- **Events Consumed**: None (foundational module)
- **Database Ownership**: Tables for regions, agencies, routes, route_variants, common_sections, frequencies, imported_feeds
- **Module Boundary Enforcement**: Spring Modulith verification in CI/CD; no cross-module database access
- **Dependencies**: No cross-module database dependencies; communicates via published events and REST APIs

**Gate Status**: ✅ PASS - All constitutional principles satisfied

## Project Structure

### Documentation (this feature)

```text
specs/003-transit-route-frequency/
├── spec.md              # Feature specification (/speckit.specify output)
├── plan.md              # This file (/speckit.plan output)
├── research.md          # Phase 0 research findings (generated below)
├── data-model.md        # Phase 1 data model design (generated below)
├── quickstart.md        # Phase 1 developer guide (generated below)
├── contracts/           # Phase 1 API contracts (generated below)
│   ├── region-api.yaml
│   ├── agency-api.yaml
│   └── frequency-api.yaml
└── checklists/
    └── requirements.md  # Spec quality validation
```

### Source Code (repository root)

```text
backend/
└── src/main/kotlin/com/mobilispect/
    └── transitanalysis/          # Spring Modulith module
        ├── TransitAnalysisModule.kt    # Module configuration
        ├── domain/                      # Domain model (private)
        │   ├── model/
        │   │   ├── Region.kt
        │   │   ├── Agency.kt
        │   │   ├── Route.kt
        │   │   ├── RouteVariant.kt
        │   │   ├── CommonSection.kt
        │   │   ├── Frequency.kt
        │   │   └── valueobjects/      # Value classes for IDs
        │   ├── service/
        │   │   ├── FeedImportService.kt
        │   │   ├── VariantIdentificationService.kt
        │   │   ├── FrequencyCalculationService.kt
        │   │   └── CommonSectionDetectionService.kt
        │   └── repository/            # Spring Data JPA repositories
        ├── application/               # Application services (public)
        │   ├── RegionService.kt
        │   ├── AgencyService.kt
        │   ├── FrequencyQueryService.kt
        │   └── FeedManagementService.kt
        ├── infrastructure/            # Infrastructure concerns
        │   ├── gtfs/                 # GTFS parsing
        │   ├── transitland/          # Transitland API client
        │   └── cache/                # Redis caching
        ├── api/                      # REST controllers (public)
        │   ├── RegionController.kt
        │   ├── AgencyController.kt
        │   ├── FrequencyController.kt
        │   └── dto/                  # API DTOs
        └── events/                   # Domain events (public)
            ├── FeedImportCompleted.kt
            ├── FrequencyCalculationCompleted.kt
            └── RouteVariantIdentified.kt

backend/src/test/kotlin/com/mobilispect/transitanalysis/
├── module/                       # Module tests (@ModuleTest)
├── integration/                  # Integration tests
├── contract/                     # API contract tests
└── unit/                        # Unit tests

frontend/
└── src/app/
    └── transit-frequency/        # Feature module
        ├── transit-frequency.module.ts
        ├── pages/
        │   ├── region-list/
        │   ├── agency-detail/
        │   └── route-frequency/
        ├── components/
        │   ├── frequency-chart/
        │   ├── variant-list/
        │   └── common-section-display/
        ├── services/
        │   ├── region.service.ts
        │   ├── agency.service.ts
        │   └── frequency.service.ts
        └── models/
            └── transit-frequency.model.ts

frontend/src/app/transit-frequency/**/*.spec.ts  # Unit tests
e2e/transit-frequency.spec.ts                     # Playwright E2E tests
```

**Structure Decision**: Web application structure selected based on Spring Boot backend + Angular frontend stack. Backend follows Spring Modulith module conventions with package-based module `transitanalysis`. Frontend uses Angular feature module pattern for frequency analysis UI. Module boundaries enforced via Spring Modulith's `@Modulith` annotations and runtime verification.

## Complexity Tracking

> **No violations - this section intentionally left empty**

All constitutional requirements are satisfied without exceptions. Module design aligns with Spring Modulith patterns, test coverage meets 80%+ threshold, observability is comprehensive, and performance targets are achievable within stated constraints.
